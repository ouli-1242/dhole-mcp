# comment/docstring references to identifiers not found in src/ (130 hits)

__init__.py:26	F401	# F401：这些名字通过下方 locals()[name] 返回，是刻意的惰性再导出，
__init__.py:28	F401	# noqa: F401
browser.py:51	suppress	# Args to suppress (scrapling found these enable automation signals)
browser.py:527	hangs	# On Windows, chrome.exe --version hangs (starts the browser).
browser.py:1089	add_init_script	# Page.addScriptToEvaluateOnNewDocument (not context.add_init_script,
browser.py:1216	add_init_script	# patchright (it patches Runtime.enable). add_init_script uses
browser.py:1257	add_init_script	# which patchright patches out; add_init_script uses Routes
browser.py:1265	IMMEDIATELY	# Inject stealth patches IMMEDIATELY after commit, before
browser.py:1364	ERR_ABORTED	# 主文档被 SSRF 守卫拦下时，goto 会以 ERR_ABORTED 之类的形式报出来 ——
browser.py:1445	F401	# noqa: F401
cache.py:186	requested_ttl	# Use MIN(stored_ttl, requested_ttl) so caller can request fresher data
cache.py:186	stored_ttl	# Use MIN(stored_ttl, requested_ttl) so caller can request fresher data
crawl.py:416	S310	# noqa: S310
crawl.py:656	anchor_text	# Heap entries: (score, depth, seq, url, anchor_text). `seq` breaks ties so
crawl.py:759	layers	# Discover links for deeper layers (skip in selective mode).
envelope.py:83	ITSELF	# Government / education / github: the only classes where the NAME ITSELF
envelope.py:96	SHAPE	# Vendor docs subdomains (docs.* / developer.*). This is a SHAPE signal, not
envelope.py:216	README	# not match the bare word "paywall": it can appear in ordinary README links,
envelope.py:378	assignment	# Redirect: meta refresh or JS location assignment (and little real text).
errors.py:21	FIRST	# Proxy errors (check FIRST: "proxy connection refused" should be proxy, not conn_refused)
errors.py:21	conn_refused	# Proxy errors (check FIRST: "proxy connection refused" should be proxy, not conn_refused)
feed.py:99	RDF	# RSS 2.0 (channel/item), also handles rdf:RDF
fetcher.py:167	platforms	# elements on different platforms (e.g. ubuntu CI returns the
metadata.py:46	WOULD	# so a bare \b WOULD match there).
ocr.py:48	dpi	# Render scale for PDFium. 2.0 ≈ 144 dpi (enough for RapidOCR on typical docs);
ocr.py:85	F401	# noqa: F401
ocr.py:89	F401	# noqa: F401
ocr.py:94	F401	# noqa: F401
pdf_extractor.py:59	thresholds	# Heading detection thresholds (ratio of a line's dominant font size to the
pdf_extractor.py:346	__len__	# len; this guards exotic wrappers / test fakes without __len__).
pdf_extractor.py:387	ORIGINAL	# printable is over the cid-stripped text; score it against the ORIGINAL len
pdf_extractor.py:517	MIXED	# image-only pages in a MIXED PDF (per-page OCR)
pdf_extractor.py:569	RAW	# Quality score from RAW page text + OCR recovery, so the honest markers
reddit.py:25	LEADING	# writes it as `class=" thing id-t3_..."` (LEADING SPACE), so the pattern allows
reddit.py:25	SPACE	# writes it as `class=" thing id-t3_..."` (LEADING SPACE), so the pattern allows
reddit.py:25	t3_	# writes it as `class=" thing id-t3_..."` (LEADING SPACE), so the pattern allows
reranker.py:54	LFS	# 的是 git 里提交的 LFS 指针（`oid sha256:...`，与下载端点不同的路径），取值后
reranker.py:59	Registry	# Registry (key order = display order). The default is DEFAULT_MODEL below.
reranker.py:76	LFS	# LFS oid @ 上面的 rev，取自 hf-mirror 的仓库元数据；与本机下载的
reranker.py:83	XLM	# Cross-lingual MiniLM distilled from XLM-R Large on mMARCO (incl. Chinese),
reranker.py:126	constants	# Backward-compat aliases for the pre-registry constants (tests + external
reranker.py:358	BERT	# vocab.txt 被 :375 那个 BERT WordPiece 回退路径使用，却不在必需列表里 ——
reranker.py:418	F401	# noqa: F401
reranker.py:419	F401	# noqa: F401
reranker.py:420	F401	# noqa: F401
reranker.py:436	BERT	# Fallback: build a BERT WordPiece tokenizer from vocab.txt.
reranker.py:479	AWAITED	# parallel search) is AWAITED rather than raced: the top-level fast path only
reranker.py:487	FINISHED	# a previous load FINISHED and failed; don't retry this process
search.py:66	mining	# ─── related-query mining (extractive, no LLM) ──────────────────────────────
search.py:86	OPENS	# A phrase that OPENS with one of these is a sentence fragment, not something a
search.py:893	STRONGER	# surfaced by different queries from different engines is a STRONGER authority
search.py:898	expensive	# negatives are cheap (no expansion); false positives are expensive (dilute
search.py:1168	engine_fetch	# AFTER it (the old path paid engine_fetch + model_load sequentially = ~6-7s
search.py:1168	model_load	# AFTER it (the old path paid engine_fetch + model_load sequentially = ~6-7s
search.py:1191	SOURCE	# Rerank candidates against the SOURCE page content (Exa find-similar,
search.py:1192	source_content	# local: the cross-encoder scores (source_content, candidate)).
search_engines.py:59	test_default_pool_definitions_agree	# tests/test_engine_registry.py::test_default_pool_definitions_agree 钉住一致性。
search_engines.py:59	test_engine_registry	# tests/test_engine_registry.py::test_default_pool_definitions_agree 钉住一致性。
search_engines.py:176	SERL	# rotation pool kept here (not imported from the removed SERL module)
search_metasearch.py:432	ARG002	# noqa: ARG002
search_metasearch.py:491	ARG002	# noqa: ARG002
search_metasearch.py:492	ARG002	# noqa: ARG002
search_metasearch.py:517	ARG002	# noqa: ARG002
search_metasearch.py:518	ARG002	# noqa: ARG002
search_metasearch.py:561	ARG002	# noqa: ARG002
search_metasearch.py:562	ARG002	# noqa: ARG002
search_metasearch.py:585	Mojeek	# ─── Mojeek (independent index) ──────────────────────────────────────────────
search_metasearch.py:597	ARG002	# noqa: ARG002
search_metasearch.py:598	ARG002	# noqa: ARG002
search_metasearch.py:678	ARG002	# noqa: ARG002
search_metasearch.py:741	test_engine_registry	# 同样的惰性导入理由，由 tests/test_engine_registry.py::test_vertical_sets_agree 钉住。
search_metasearch.py:741	test_vertical_sets_agree	# 同样的惰性导入理由，由 tests/test_engine_registry.py::test_vertical_sets_agree 钉住。
search_metasearch.py:1491	MIN_ENGINES	# Diversity quorum: wait for at least MIN_ENGINES backends to contribute
search_metasearch.py:1494	SOFT_DEADLINE	# fallback returns at SOFT_DEADLINE once we have enough results even if some
security.py:23	targeted	# Hostnames that must never be targeted (bypass IP checks via name resolution)
security.py:26	brackets	# "0" from bare IPv6 without brackets (e.g. http://0:0:0:...)
security.py:83	TOCTOU	# 只缓存几秒（覆盖一条重定向链，又不至于把 DNS rebinding 的 TOCTOU 窗口
security.py:250	octal	# Case: pure decimal integer (2130706433) or packed octal (017700000001)
security.py:265	inet_aton	# curl/libcurl's inet_aton accepts this form; ipaddress rejects it. Without
security.py:288	octal	# Leading zero → octal (curl behavior): '0177' → 127
security.py:297	inet_aton	# Expand short-form to 4 octets (curl/inet_aton behavior).
security.py:297	octets	# Expand short-form to 4 octets (curl/inet_aton behavior).
security.py:348	CRITICAL	# CRITICAL: Reject URLs containing backslash in authority section.
security.py:382	IPvFuture	# Allow IPvFuture (v[0-9]+\..+) but nothing else
security.py:443	hostnames	# Block known internal hostnames (cloud metadata, localhost, DNS rebinding services)
security.py:453	TOCTOU	# 存在 DNS rebinding TOCTOU 竞态，作为纵深防御而非唯一防线。
security.py:495	backtracking	# Block selectors that could cause excessive backtracking (ReDoS)
server.py:60	f_code	# extract_tb yields FrameSummary objects (.filename, not .f_code).
server.py:200	LRU	# Cap dict size to prevent unbounded growth (LRU-like: clear oldest half)
server.py:200	growth	# Cap dict size to prevent unbounded growth (LRU-like: clear oldest half)
server.py:248	ONCE	# MCP initialize `instructions` — injected into the agent's context ONCE on
server.py:251	FIRST	# imperatives with the "prefer dhole over built-ins" rule FIRST, because tool
server.py:428	CONFIRMATION	# Looser vocabulary used only as a CONFIRMATION when the URL already points at
server.py:461	ABOUT	# Length-gated on purpose: a status-200 article ABOUT captchas is not a wall.
server.py:982	FOCUSED	# so offset/next_offset page through the FOCUSED content.
server.py:994	HAD	# page that HAD text deserves the "no more content" marker (the agent
server.py:1352	P14	# redirect. Don't extract the login HTML as if it were content (P6/P14).
server.py:1457	enrichment	# Metadata enrichment (OpenGraph + JSON-LD + canonical + <title>) for HTML
server.py:1732	ALLOWED	# Per tool: ALLOWED = every key that may appear in options (documented +
server.py:1733	FORWARDED	# promoted-fallback keys), FORWARDED = the subset actually passed through
server.py:2256	CLEAN	# Only cache CLEAN content. Caching JS shells / bot challenges / geo
server.py:2262	occasionally	# Docs rarely change (24h); articles change occasionally (6h);
server.py:2264	respected	# always respected (cache_ttl != DEFAULT_TTL means user chose it).
server.py:2936	FETCH	# ─── SMART FETCH (The One Tool To Rule Them All) ────────────────
server.py:2936	SMART	# ─── SMART FETCH (The One Tool To Rule Them All) ────────────────
server.py:3043	raising	# Clamp to [500, 200000] instead of raising (avoids Parse Error)
server.py:3073	compliance	# Robots.txt compliance (must check before fetching)
server.py:3339	ORIGINAL	# report the ORIGINAL url, not the archive url
server.py:3365	WHOLE	# `timeout` is the caller's wall-clock budget for the WHOLE call, and it
server.py:3450	THIS	# Capped by what is left of THIS call: "at least 20s" used to mean a call
server.py:4178	EVERYTHING	# swallowing EVERYTHING (including 'Event loop is closed' and
server.py:4324	SDK	# structured_content 只接受 dict，list 会触发 MCP SDK 校验错误
structured.py:155	article_author	# Tail-segment match: "og:title" -> "title", "article_author" -> "author"
trafilatura_extractor.py:55	BBC	# Clean up common suffixes like " - Wikipedia", " | BBC News", etc.
trafilatura_extractor.py:303	WHOLE	# which made the caller fall back to the WHOLE page — the exact silent no-op
trafilatura_extractor.py:362	SOME	# regardless of requested type, just to get SOME clean content
updater.py:55	THIS	# PyPI; ``dhole-mcp`` is now the distribution name of THIS fork. Self-update
updater.py:158	helpers	# ─── launcher + process helpers (Windows file-lock handling) ───────────────
updater.py:195	CREATE_NO_WINDOW	# CREATE_NO_WINDOW
updater.py:196	GBK	# Windows 控制台程序按 OEM 代码页输出，中文系统上是 GBK，而
updater.py:196	OEM	# Windows 控制台程序按 OEM 代码页输出，中文系统上是 GBK，而
updater.py:245	SIGTERM	# SIGTERM
updater.py:594	UPSTREAM	# UPSTREAM distribution, which is not this fork.
updater.py:714	ERROR	# Every optional piece below turns a headline feature OFF WITHOUT AN ERROR:
updater.py:972	BLE001	# noqa: BLE001
updater.py:987	BLE001	# noqa: BLE001
updater.py:1005	BLE001	# noqa: BLE001
updater.py:1016	BLE001	# noqa: BLE001
updater.py:1041	BLE001	# noqa: BLE001
updater.py:1056	BLE001	# noqa: BLE001
